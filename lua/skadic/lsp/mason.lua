local opts = {
  ui = {
    icons = {
      package_installed = "",
      package_pending = "",
      package_uninstalled = ""
    }
  },
  PATH = "prepend"
}

return opts
--require("skadic.lsp.mason-lspconfig")
