local opts = {
  ui = {
    icons = {
      package_installed = "󰏓",
      package_pending = "󰏓",
      package_uninstalled = ""
    }
  },
  PATH = "prepend"
}

return opts
--require("basic.lsp.mason-lspconfig")
